Custom Minecraft Clone

Visual Studio: make sure you have out/x64_release and out/x64_debug directories, and also in each configuration make sure you have world/ directory, in which saves are saved (the same for Linux, VS Code, except for separate configuration directories)